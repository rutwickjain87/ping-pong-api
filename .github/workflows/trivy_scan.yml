name: Trivy Vulnerability Scanner

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]

jobs:
  trivy-scan:
    name: Trivy vulnerability scanner
    runs-on: ubuntu-latest
    # outputs:
    #   docker_image_list: ${{ steps.docker_image_list.outputs.DOCKER_IMAGE_LIST }}
    
    steps:
    - name: Install jq
      run: sudo apt-get update && sudo apt-get install -y jq
      shell: bash

    - name: Checkout code
      uses: actions/checkout@v4

    - name: Print List
      env:
        MY_LIST: '"me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/ais:1.2.9","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/auth:1.2.10","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/cron_job:1.1.7","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/fx:1.1.2","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/kms:1.1.2","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/management:1.3.13","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/transaction_persist:1.1.9","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/account_fetcher:1.2.10","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/discovery:1.0.0","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/api_auth:1.1.2","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/mail:1.1.2","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/webhook:1.1.3","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/terms:0.0.0","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/ob_api_monitoring:1.1.3","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/ais_web:1.1.8","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/account_verification:1.1.3","me-central2-docker.pkg.dev/spare-ksa/sa-universal-api/beneficiary_fetcher:1.1.6"'
      run: bash .github/scripts/print-list.sh
      shell: bash

    - name: Set color
      id: color-selector
      run: echo "SELECTED_COLOR=green" >> "$GITHUB_OUTPUT"

    - name: Get color
      env:
        SELECTED_COLOR: ${{ steps.color-selector.outputs.SELECTED_COLOR }}
      run: echo "The selected color is $SELECTED_COLOR"
      
    - name: Set the value
      id: step_one
      run: |
        echo "action_state=yellow" >> "$GITHUB_ENV"
    - name: Use the value
      id: step_two
      run: |
        printf '%s\n' "$action_state" # This will output 'yellow'

    - name: Generate Docker Image list for build=true services
      id: docker_image_list
      run: |
        list=$(jq -cr '[.services[] | select(.build == false) | (.registry + "/" + .name  + ":" + .version)]' < deployment.json | sed "s/\[\|\]/\'/g")
        echo $list
        echo "DOCKER_IMAGE_LIST=$list" >> "$GITHUB_OUTPUT"

    - name: Show Docker Image list
      run: |
        #echo "Output: ${{ steps.docker_image_list.outputs.DOCKER_IMAGE_LIST }}"
        printf '%s\n' "$DOCKER_IMAGE_LIST"

    - name: Loop through paths
      id: loop
      run: |
        for i in "${{ steps.docker_image_list.outputs.DOCKER_IMAGE_LIST }}"; do
          echo "Current path: $i"
          # Add your logic here for each path
        done
    # - name: Extract version from deploymen
    #     uses: sergeysova/jq-action@v2
    #     id: generate
    #     with:
    #       cmd: jq -r '.services[] | select(.build == false) |  {image: (.registry + "/" + .name  + ":" + .version)} ' < ./deployment.json
      
    # - name: Show my version
    #     run: 'echo "version ${{ steps.version.outputs.value }}"'

    # - name: generate docker image list
      
      
    - name: Build Docker image
      run: docker build -t ping-pong-api:${{ github.sha }} .
      
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: 'ping-pong-api:${{ github.sha }}'
        format: 'sarif'
        output: 'trivy-results.sarif'
        severity: 'CRITICAL,HIGH,MEDIUM,LOW'
        
    - name: Upload Trivy scan results
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: 'trivy-results.sarif'
